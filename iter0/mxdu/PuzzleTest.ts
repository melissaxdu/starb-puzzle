import assert from 'assert';
import fs from 'fs';

/**
 * Tests for the Puzzle abstract data type.
 */

describe('Puzzle', function () {
    /*
    * Testing strategy for Puzzle
    * 
    * partition on starsNeeded: 1, 2, 3
    * partition on rows and cols: rows === cols, rows !== cols
    * partition on isWon: true, false 
    */
});